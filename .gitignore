# Local data — never commit
input/
output/

# Local env/config (keep .env.example)
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Docker work artifacts (if any leak to host)
work/
*.wav

# Comparison run data (generated transcripts — do not commit). The comparison
# tool itself (cmp/compare.py) IS tracked.
cmp/sherpa/
cmp/pyannote3/
cmp/pyannote4/
